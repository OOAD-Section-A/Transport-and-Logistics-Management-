# Database Configuration for Transport & Logistics Module
# NOTE: Update these values with your actual database credentials before running integration tests
# These credentials are used to connect to the SCM database module

db.url=jdbc:mysql://localhost:3306/OOAD
db.username=root
db.password=your_password_here
